# Copy this file to .env and fill in your real values.
# .env is git-ignored and must never be committed.

FIRECRAWL_API_KEY=
PERPLEXITY_API_KEY=

# Optional: run Claude Code through OmniRoute as the API backend.
# Create the key in the OmniRoute dashboard (API Keys) and connect a provider first.
ANTHROPIC_BASE_URL=http://localhost:20128/v1
ANTHROPIC_API_KEY=
